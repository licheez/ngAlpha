import { EmsCustomerHttpClient } from './ems-customer-http-client';

describe('EmsCustomerHttpClient', () => {
  it('should create an instance', () => {
    expect(new EmsCustomerHttpClient()).toBeTruthy();
  });
});
